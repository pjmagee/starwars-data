repos:
  - repo: local
    hooks:
      - id: dotnet-build
        name: dotnet build
        entry: dotnet build src/StarWarsData.slnx --no-restore -c Release
        language: system
        pass_filenames: false
        files: \.(cs|razor|csproj|slnx)$
