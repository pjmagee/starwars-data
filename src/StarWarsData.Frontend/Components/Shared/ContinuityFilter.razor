@using StarWarsData.Models.Entities
@using StarWarsData.Frontend.Services
@implements IDisposable

<MudStack Row="true" AlignItems="AlignItems.Center" Spacing="3" Class="mr-4">
    <MudSwitch
        T="bool"
        Value="_canonEnabled"
        ValueChanged="OnCanonChanged"
        Label="Canon"
        Color="Color.Primary"
        Size="Size.Small"
        Class="ma-0" />

    <MudSwitch
        T="bool"
        Value="_legendsEnabled"
        ValueChanged="OnLegendsChanged"
        Label="Legends"
        Color="Color.Secondary"
        Size="Size.Small"
        Class="ma-0" />

    <MudDivider Vertical="true" FlexItem="true" />

    <MudSwitch
        T="bool"
        Value="_starwarsEnabled"
        ValueChanged="OnStarwarsChanged"
        Label="Star Wars"
        Color="Color.Tertiary"
        Size="Size.Small"
        Class="ma-0" />

    <MudSwitch
        T="bool"
        Value="_realEnabled"
        ValueChanged="OnRealChanged"
        Label="Real World"
        Color="Color.Warning"
        Size="Size.Small"
        Class="ma-0" />
</MudStack>

@code {
    private bool _canonEnabled = true;
    private bool _legendsEnabled = true;
    private bool _starwarsEnabled = true;
    private bool _realEnabled = false;

    [Inject] private GlobalFilterService GlobalFilterService { get; set; } = default!;

    protected override void OnInitialized()
    {
        UpdateSwitchesFromService();
        GlobalFilterService.OnChange += OnFilterServiceChanged;
    }

    public void Dispose()
    {
        GlobalFilterService.OnChange -= OnFilterServiceChanged;
    }

    private void UpdateSwitchesFromService()
    {
        switch (GlobalFilterService.SelectedContinuity)
        {
            case Continuity.Canon:
                _canonEnabled = true;
                _legendsEnabled = false;
                break;
            case Continuity.Legends:
                _canonEnabled = false;
                _legendsEnabled = true;
                break;
            default:
                _canonEnabled = true;
                _legendsEnabled = true;
                break;
        }

        switch (GlobalFilterService.SelectedRealm)
        {
            case Realm.Starwars:
                _starwarsEnabled = true;
                _realEnabled = false;
                break;
            case Realm.Real:
                _starwarsEnabled = false;
                _realEnabled = true;
                break;
            default: // null = both
                _starwarsEnabled = true;
                _realEnabled = true;
                break;
        }
    }

    private async void OnFilterServiceChanged()
    {
        UpdateSwitchesFromService();
        await InvokeAsync(StateHasChanged);
    }

    private void OnCanonChanged(bool value)
    {
        _canonEnabled = value;
        UpdateContinuityService();
    }

    private void OnLegendsChanged(bool value)
    {
        _legendsEnabled = value;
        UpdateContinuityService();
    }

    private void OnStarwarsChanged(bool value)
    {
        _starwarsEnabled = value;
        // If user just turned Star Wars off and Real was also off, auto-enable Real
        // so at least one realm is always visible. Mirrors the Canon/Legends default
        // (which defaults both-on, so turning one off flips cleanly to the other).
        if (!_starwarsEnabled && !_realEnabled)
            _realEnabled = true;
        UpdateRealmService();
    }

    private void OnRealChanged(bool value)
    {
        _realEnabled = value;
        if (!_starwarsEnabled && !_realEnabled)
            _starwarsEnabled = true;
        UpdateRealmService();
    }

    private void UpdateContinuityService()
    {
        if (!_canonEnabled && !_legendsEnabled)
        {
            _canonEnabled = true;
            _legendsEnabled = true;
        }

        Continuity? newContinuity = (_canonEnabled, _legendsEnabled) switch
        {
            (true, false) => Continuity.Canon,
            (false, true) => Continuity.Legends,
            _ => null
        };

        GlobalFilterService.SetContinuity(newContinuity);
    }

    private void UpdateRealmService()
    {
        // The per-switch handlers guarantee at least one side is on, so no
        // fallback coercion is needed here — if both were off the handler
        // that triggered this call already flipped the other side on.
        Realm? newRealm = (_starwarsEnabled, _realEnabled) switch
        {
            (true, false) => Realm.Starwars,
            (false, true) => Realm.Real,
            _ => null
        };

        GlobalFilterService.SetRealm(newRealm);
    }
}
