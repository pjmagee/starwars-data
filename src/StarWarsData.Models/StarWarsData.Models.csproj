<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>preview</LangVersion>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.VectorData.Abstractions" Version="10.8.2" />
    <!-- Direct pin: VectorData.Abstractions 10.8.2 depends on AI.Abstractions >= 10.8.2, but 10.8.2 was never published (10.8.0/1/3 only). Without this, NU1603 near-version resolve to 10.8.3 fails under TreatWarningsAsErrors. -->
    <PackageReference Include="Microsoft.Extensions.AI.Abstractions" Version="10.8.3" />
    <PackageReference Include="MongoDB.Bson" Version="3.10.0" />
  </ItemGroup>
</Project>
